package SnakeGame.Model;
/**
 * GameState is an enum class that contains the different states of the game
 */

import processing.core.PApplet;

public enum GameState {
    MENU,
    PLAYING,
    GAMEOVER,
    WIN
}
